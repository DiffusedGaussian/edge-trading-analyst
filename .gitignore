.venv/
__pycache__/
*.pyc
.DS_Store
data/*.db
data/testfile

# Ad-hoc Tier 0 run output. baseline.json is the one committed reference run
# (see eval/results/README.md) — add it explicitly with `git add -f`.
eval/results/*.json
!eval/results/baseline.json
